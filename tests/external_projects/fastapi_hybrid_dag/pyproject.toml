[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "external-hybrid-api"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.111,<0.116",
    "uvicorn>=0.29,<0.31",
]
