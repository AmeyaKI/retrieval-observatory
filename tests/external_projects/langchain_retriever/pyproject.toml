[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "external-langchain-retriever"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["langchain-core>=0.2,<0.4"]
