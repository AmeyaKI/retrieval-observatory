[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "external-llamaindex-retriever"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["llama-index-core>=0.10,<0.13"]
